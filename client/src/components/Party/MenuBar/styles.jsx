import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles((theme) => ({
  exitButton: {
    marginLeft: 'auto',
  },

  roomTitle: {
    fontFamily: 'arial',
    fontWeight: '600',
  },
}))

export default useStyles
